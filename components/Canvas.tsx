
import React from 'react';

// This component is no longer used in the current layered flow architecture.
// It referred to a 'WorkNode' type and 'nodes' property that are not present in the current model.
const Canvas: React.FC<any> = () => null;

export default Canvas;
